extends baseTemplate

block content 
  h1 This is dynamic LoanList Overview
  div
    table.table-header
      thead
        tr
          th(scope='col') Name
          th(scope='col') loanType
          th(scope='col') loanNumber
          th(scope='col') Amount
          th(scope='col') interestRate
          th(scope='col') loanTermYears
      tbody
        //- each course in courses 
        //-   tr.table-secondary
        //-     td=course.id
        //-     td=course.courseName
        //-     td=course.instructor
        //-     td=course.credits
        //-     td=course.from